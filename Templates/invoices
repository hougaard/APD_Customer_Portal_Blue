<!DOCTYPE html>
<html lang="en">
<head>
  <title>Invoices - Customer Portal</title>
  <?parse('portal-head')?>
</head>
<body>
  <?parse('portal-header')?>

  <?table("Sales Invoice Header")?>
  <?filter("Sales Invoice Header","Sell-to Customer No.",no())?>

  <section class="section">
    <div class="container">
      <div class="is-flex is-justify-content-space-between is-align-items-center mb-5">
        <div>
          <h1 class="page-title"><i class="fas fa-file-invoice mr-2" style="color:var(--success);"></i>Invoices</h1>
          <p class="page-subtitle">Download your posted invoices as PDF.</p>
        </div>
        <a href="<?action('DASHBOARD')?>" class="button btn-ghost">
          <span class="icon"><i class="fas fa-arrow-left"></i></span><span>Back to Dashboard</span>
        </a>
      </div>

      <div class="dark-card p-5">
        <?if("Sales Invoice Header",'COUNT','>','0')?>
        <div class="table-container">
          <table class="table is-fullwidth dark-table">
            <thead>
              <tr>
                <th>Invoice No.</th>
                <th>Date</th>
                <th>Due Date</th>
                <th>Your Reference</th>
                <th class="has-text-right">Amount Incl. VAT</th>
                <th class="has-text-centered">PDF</th>
              </tr>
            </thead>
            <tbody>
              <?loop("Sales Invoice Header",'+')?>
              <tr>
                <td style="color:var(--accent-light);font-weight:600;"><?field("Sales Invoice Header","No.")?></td>
                <td><?field("Sales Invoice Header","Posting Date")?></td>
                <td><?field("Sales Invoice Header","Due Date")?></td>
                <td><?field("Sales Invoice Header","Your Reference")?></td>
                <td class="has-text-right" style="font-weight:600;color:var(--text-bright);"><?field("Sales Invoice Header","Amount Including VAT")?></td>
                <td class="has-text-centered">
                  <a href="<?action('INVOICEPDF',"Sales Invoice Header")?>" target="_blank" class="button is-small btn-primary" title="Download PDF">
                    <span class="icon"><i class="fas fa-file-pdf"></i></span>
                    <span>PDF</span>
                  </a>
                </td>
              </tr>
              <?endloop("Sales Invoice Header",-1)?>
            </tbody>
          </table>
        </div>
        <?else?>
        <p style="color:var(--text-muted);text-align:center;padding:2rem;"><i class="fas fa-inbox mr-2"></i>No invoices found.</p>
        <?endif?>
      </div>

    </div>
  </section>

  <?close("Sales Invoice Header")?>
  <?parse('portal-footer')?>
</body>
</html>